import PropTypes from 'prop-types'

function Gallery({ pictures }) {
  return <div>{/* carroussel pour les photos */}</div>
}

Gallery.propTypes = {
  pictures: PropTypes.string,
}

export default Gallery
